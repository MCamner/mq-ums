{
  "name": "mq-ums",
  "version": "0.1.4",
  "description": "IGEL UMS management web interface via PSIGEL",
  "main": "server/src/index.js",
  "scripts": {
    "start": "node server/src/index.js",
    "dev": "node --watch server/src/index.js",
    "test": "node --test tests/*.test.js",
    "validate": "node server/src/validate-config.js",
    "validate:commands": "node server/src/validate-config.js",
    "validate:contracts": "python3 tools/validate-command-contracts.py"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
